import type { WhatsAppProvider } from '../types'

// Placeholder to make provider dispatch explicit and additive.
// Existing Meta integration remains in the current whatsapp modules.
export const metaProvider: WhatsAppProvider = {
  name: 'meta',
}
